apply plugin: 'com.android.application'

android {
    compileSdkVersion 22
    buildToolsVersion "22.0.1"

    defaultConfig {
        applicationId "com.livae.android.loading.test"
        minSdkVersion 15
        targetSdkVersion 22
    }
}

dependencies {
    compile project(':library')
}
